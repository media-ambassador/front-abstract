import { MaSliderTopLayerDirective } from './slider-top-layer.directive';

describe('SliderTopLayerDirective', () => {
  it('should create an instance', () => {
    const directive = new MaSliderTopLayerDirective();
    expect(directive).toBeTruthy();
  });
});
