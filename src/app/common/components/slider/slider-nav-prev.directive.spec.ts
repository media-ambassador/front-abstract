import { MaSliderNavPrevDirective } from './slider-nav-prev.directive';

describe('SliderNavPrevDirective', () => {
  it('should create an instance', () => {
    const directive = new MaSliderNavPrevDirective();
    expect(directive).toBeTruthy();
  });
});
