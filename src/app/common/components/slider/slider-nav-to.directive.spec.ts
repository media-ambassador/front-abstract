import { MaSliderNavToDirective } from './slider-nav-to.directive';

describe('SliderNavToDirective', () => {
  it('should create an instance', () => {
    const directive = new MaSliderNavToDirective();
    expect(directive).toBeTruthy();
  });
});
