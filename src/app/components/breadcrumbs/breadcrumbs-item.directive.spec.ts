import { MaBreadcrumbsItemDirective } from './breadcrumbs-item.directive';

describe('BreadcrumbsItemDirective', () => {
  it('should create an instance', () => {
    const directive = new MaBreadcrumbsItemDirective();
    expect(directive).toBeTruthy();
  });
});
