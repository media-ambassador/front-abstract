/**
 * Wskazuje template do użycia
 *
 * zmienne (context) przekazywane do template:
 *
 * ($implicit: item), index, first, last, odd, even
 *
 */
export declare class MaBreadcrumbsItemDirective {
    constructor();
}
